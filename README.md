# Linked-Lists
Java sorted linked list with ordered insert, delete, and display. Includes OddGame, an interactive mode where divisibility rules trigger different list operations until the node sum reaches 100.
